# Yunti LangBot Sales Sanitized Data Package

This archive contains a sanitized copy of `data/` for teammate handoff.

Sanitized items include model provider API keys, service API keys, user password/token fields, JWT secret, recovery key, and configured database/vector-store passwords.

The original local `data/` directory was not modified.
